import React from "react";
import ReactDOM from "react-dom/client";
import AgentOps from "./AgentOps.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AgentOps />
  </React.StrictMode>
);
